# Machine-local Claude Code settings (permissions differ per machine)
.claude/settings.local.json

# Playwright MCP session artifacts
.playwright-mcp/

# Scratch / verification artifacts
.shots/
*.b64
*.png

# OS / editor cruft
Thumbs.db
desktop.ini
.DS_Store
*.swp
*.pem
.idea/
.vscode/
.history

# Env — never commit real keys
.env*.local

# Vercel CLI project link (repo root — CLI must run from here, not apps/web,
# see docs/backend-bootstrap.md Phase C). Per-machine, not shared.
.vercel/

# Node
node_modules/
dist/
.next/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.tsbuildinfo
/coverage
