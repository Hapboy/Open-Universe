.vercel
.env*

# Auto-regenerated by `next dev`/`next build`; the referenced routes.d.ts
# path flips between dev and build modes (Turbopack), so tracking it just
# produces no-op diffs depending on which command last ran.
next-env.d.ts
